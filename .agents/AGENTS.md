# Git Workflow Rule

After completing any task, you must automatically commit and push your changes to GitHub:
1. Stage all changes: `git add .`
2. Commit with a descriptive message outlining what actually changed: `git commit -m "..."`
3. Push to the main branch: `git push origin main`
4. If a push error occurs, report it immediately with the exact error.
